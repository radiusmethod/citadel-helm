apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "citadel.fullname" . }}-test-connection"
  labels:
    {{- include "citadel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "citadel.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
